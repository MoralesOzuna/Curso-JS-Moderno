const puntaje = 1000;


if(puntaje == 1001){
    console.log('Si es igual'); //Si se cumple la condicion se ejecuta este codigo
} else{ 
    console.log('No es igual'); //Si no se cumple, se ejecuta esto
}